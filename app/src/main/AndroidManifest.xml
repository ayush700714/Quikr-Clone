<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quikrclone">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuikrClone">
        <activity android:name=".onboarding.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CommentActivity" />
        <activity android:name=".AddNewPostActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".onboarding.LoginActivity">

        </activity>
        <activity android:name=".onboarding.OtpActivity" />
        <activity android:name=".onboarding.SignUpActivity" />
        <activity android:name=".onboarding.SignUp2Activity" />
        <activity android:name=".onboarding.Signup3Activity" />
    </application>

</manifest>